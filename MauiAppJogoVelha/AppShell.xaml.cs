namespace MauiAppJogoVelha // Define um novo namespace chamado MauiAppJogoVelha.
{
    public partial class AppShell : Shell // Declara a classe AppShell que herda da classe Shell, que é usada para criar uma estrutura de navegação.
    {
        public AppShell() // Construtor da classe AppShell.
        {
            InitializeComponent(); // Inicializa os componentes da interface definidos em XAML.
        }
    }
}
